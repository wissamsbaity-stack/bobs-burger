export const SPLASH_STORAGE_KEY = "burgerhub-splash-seen";

/** Fade-in duration (ms) — matches CSS animation */
export const SPLASH_FADE_IN_MS = 500;
/** Hold at full visibility after fade-in completes */
export const SPLASH_HOLD_MS = 1100;
/** Fade-out duration (ms) — matches CSS animation */
export const SPLASH_EXIT_MS = 350;
